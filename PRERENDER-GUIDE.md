# 🚀 Инструкция по Prerendering для SEO

## Что это?

Prerendering создаёт статические HTML-копии всех страниц сайта, чтобы поисковые боты (Google, Яндекс) и SEO-сервисы видели полный контент, а не пустую страницу.

## Как использовать

### После каждой сборки проекта:

```bash
# 1. Собрать проект
bun run build

# 2. Сгенерировать статические HTML для SEO
node generate-static.js
```

**Важно:** Запускайте `generate-static.js` после КАЖДОЙ сборки проекта!

## Какие страницы генерируются?

Список страниц находится в файле `generate-static.js`:

- `/` — Главная
- `/services` — Услуги
- `/promotions` — Акции
- `/reviews` — Отзывы
- `/blog` — Блог
- `/brands` — Бренды
- `/legal` — Юридическая информация
- `/bonus-program` — Бонусная программа
- `/warranty` — Гарантия

## Как добавить новые страницы?

Откройте `generate-static.js` и добавьте новый маршрут в массив `routes`:

```javascript
const routes = [
  '/',
  '/services',
  '/new-page',  // ← добавьте сюда
];
```

## Как это работает?

1. Vite собирает SPA-приложение в `/dist`
2. Скрипт `generate-static.js` берёт `index.html`
3. Создаёт копии в подпапках: `/services/index.html`, `/blog/index.html` и т.д.
4. Когда бот заходит на `https://hybrid24.ru/services`, сервер отдаёт готовый HTML
5. Для обычных пользователей React работает как SPA (без перезагрузки)

## Проверка

После генерации проверьте сайт на:
- https://checksite.ru
- Google Search Console
- Яндекс.Вебмастер

Боты теперь должны видеть полный контент! ✅
