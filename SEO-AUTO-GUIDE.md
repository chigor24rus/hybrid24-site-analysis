# 🎯 Автоматическая генерация SEO при скачивании билда

## ✅ Что готово

Создана backend-функция **post-build**, которая автоматически добавляет SEO-файлы в билд проекта.

**URL функции:** `https://functions.poehali.dev/ef9dadf3-a896-48cd-945d-e32c58eeb32f`

### Как работает

Функция принимает ZIP-архив с билдом, добавляет статические HTML-копии страниц для SEO и возвращает обновлённый архив.

## 📝 Временное решение (пока функция не интегрирована в платформу)

Пока автоматическая интеграция не добавлена в poehali.dev, используйте один из методов:

### Вариант 1: Локальная генерация (рекомендуется)

1. Скачайте код проекта с GitHub
2. В терминале выполните:
   ```bash
   bun run build
   node generate-static.js
   ```
3. Загрузите папку `dist/` на хостинг

### Вариант 2: Ручной вызов функции (для продвинутых)

Если умеете работать с API, можете вызвать функцию напрямую:

```javascript
// Скачайте билд из poehali.dev
// Закодируйте ZIP в base64
// Отправьте POST запрос:

fetch('https://functions.poehali.dev/ef9dadf3-a896-48cd-945d-e32c58eeb32f', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    buildZip: '<base64-encoded-zip>'
  })
})
.then(res => res.json())
.then(data => {
  // data.buildZip содержит обновлённый архив с SEO-файлами
  // Декодируйте из base64 и сохраните
})
```

## 🚀 Будущее (когда будет интегрировано)

В следующих версиях poehali.dev кнопка **"Скачать билд"** будет автоматически:
1. Собирать проект
2. Вызывать функцию post-build
3. Добавлять SEO-файлы
4. Отдавать готовый архив

Вам не придётся ничего делать вручную! 🎉

## Что добавляется в билд

Функция создаёт статические HTML-копии для:
- `/services` — Услуги
- `/promotions` — Акции
- `/reviews` — Отзывы
- `/blog` — Блог
- `/brands` — Бренды автомобилей
- `/legal` — Юридическая информация
- `/bonus-program` — Бонусная программа
- `/warranty` — Гарантия

Каждая страница получает свой `index.html`, чтобы поисковые боты видели полный контент.

## Техническая информация

- **Функция:** `backend/post-build/index.py`
- **Тесты:** Все тесты пройдены ✅
- **Язык:** Python 3.11
- **Формат:** Принимает и возвращает base64-encoded ZIP

---

**Совет:** Пока используйте локальную генерацию (`generate-static.js`) — это быстро и просто!
