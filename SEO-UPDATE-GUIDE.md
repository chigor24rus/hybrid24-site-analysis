# 📋 Инструкция по обновлению сайта с SEO-оптимизацией

## Когда использовать

После каждого изменения контента или структуры сайта в poehali.dev необходимо обновить версию на хостинге.

## Пошаговая инструкция

### Шаг 1: Скачать изменения с GitHub

Откройте терминал (командную строку) и выполните:

```bash
cd C:\Windows\System32\hybrid24-site-analysis
git pull
```

Это загрузит последние изменения из GitHub.

### Шаг 2: Установить зависимости (если добавлялись новые пакеты)

```bash
bun install
```

Обычно этот шаг можно пропустить, если не добавлялись новые библиотеки.

### Шаг 3: Собрать проект

```bash
bun run build
```

Это создаст оптимизированную версию сайта в папке `dist/`.

### Шаг 4: Создать SEO-версии страниц

```bash
node prerender-puppeteer.js
```

Эта команда:
- Запустит локальный сервер
- Откроет браузер в фоновом режиме
- Отрендерит все страницы с полным контентом
- Сохранит HTML-файлы для поисковиков

Вы увидите примерно такой вывод:

```
🚀 Starting pre-rendering with Puppeteer...
✓ Local server started at http://localhost:5555
⏳ Rendering: /
✓ / (saved as index.html)
⏳ Rendering: /services
✓ /services
...
✅ Pre-rendering complete!
```

### Шаг 5: Загрузить на хостинг

1. Откройте папку `C:\Windows\System32\hybrid24-site-analysis\dist\`
2. Зайдите в панель управления хостингом
3. **Удалите все старые файлы** на хостинге
4. **Загрузите всё содержимое папки `dist/`** на хостинг

### Шаг 6: Проверить результат

1. Откройте https://hybrid24.ru в браузере
2. Нажмите `Ctrl+Shift+R` (жёсткое обновление)
3. Нажмите `Ctrl+U` (просмотр исходного кода)
4. Найдите текст "Профессиональный ремонт" — он должен быть в HTML

### Шаг 7: Проверить SEO (опционально)

Проверьте сайт на https://checksite.ru — все ошибки по контенту должны быть исправлены.

---

## Быстрая шпаргалка (все команды подряд)

```bash
cd C:\Windows\System32\hybrid24-site-analysis
git pull
bun run build
node prerender-puppeteer.js
```

После этого загрузите `dist/` на хостинг.

---

## Частые проблемы

### Ошибка: "Cannot find module"
**Решение:** Запустите `bun install`

### Puppeteer не запускается
**Решение:** Убедитесь, что установлен Node.js последней версии

### Сайт показывает старую версию
**Решение:** Очистите кеш браузера (`Ctrl+Shift+R`) и проверьте, что все файлы загружены на хостинг

### На хостинге нет контента (пустая страница)
**Решение:** Убедитесь, что вы запустили `node prerender-puppeteer.js` ДО загрузки на хостинг

---

## Важно! ⚠️

- **Всегда запускайте `git pull`** перед сборкой, чтобы получить последние изменения
- **Не пропускайте** команду `node prerender-puppeteer.js` — без неё поисковики не увидят контент
- **Удаляйте старые файлы** на хостинге перед загрузкой новых

---

**Время выполнения:** 2-3 минуты  
**Частота обновления:** После каждого изменения контента в poehali.dev
